# Agent-Up Integration Evidence

This directory is copied from the Control Center source authority during export.
It documents the upstream Agent-Up corpus and rollout validators that shaped the
standalone sync-core crate.

These files are not part of the default GitHub CI because they depend on the
larger Control Center/Agent-Up runtime. The standalone crate CI runs the Rust
library, CLI parity tests, and benchmark compile smoke from the repository root.
